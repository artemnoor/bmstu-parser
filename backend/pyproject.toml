[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "university-data-platform"
version = "3.0.0"
description = "Extensible university data ingestion and normalization platform"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31,<3",
    "pdfplumber>=0.11,<1",
    "python-docx>=1.1,<2",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
    "click>=8.1,<9",
    "duckdb>=1.0,<1.2",
    "openpyxl>=3.1,<4",
    "PyYAML>=6.0,<7",
]

[project.optional-dependencies]
docling = ["docling>=2.0,<3"]
dev = [
    "mypy>=1.11,<2",
    "httpx>=0.27,<1",
    "pytest>=8.0,<9",
    "pytest-asyncio>=0.23,<1",
    "coverage>=7.5,<8",
    "ruff==0.16.5",
    "types-PyYAML>=6.0,<7",
]

[project.scripts]
university-data = "university_data.cli:main"
university-api = "university_data.api.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
university_data = [
    "universities/bmstu/config.yaml",
    "universities/bmstu/manifest.yaml",
    "universities/fake/config.yaml",
    "universities/fake/manifest.yaml",
    "universities/fake/fixtures/*.json",
    "universities/fake/fixtures/*.xlsx",
    "universities/hse/config.yaml",
    "universities/hse/manifest.yaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_default_fixture_loop_scope = "function"

[tool.coverage.run]
source = [
    "university_data.domain",
    "university_data.normalization",
    "university_data.resolvers",
    "university_data.sources.xlsx",
    "university_data.universities.bmstu.adapter.study_plans",
]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
warn_unused_ignores = true
cache_dir = ".mypy_cache"
